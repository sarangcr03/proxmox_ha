start_vm:
  description: Start a virtual machine in Proxmox.
  fields:
    vm_id:
      name: VM ID
      description: The ID of the virtual machine.
      example: "101"
      selector:
        text:
    node_name:
      name: Node Name
      description: The name of the node.
      example: "proxmox_node"
      selector:
        text:
stop_vm:
  description: Stop a virtual machine in Proxmox.
  fields:
    vm_id:
      name: VM ID
      description: The ID of the virtual machine.
      example: "101"
      selector:
        text:
    node_name:
      name: Node Name
      description: The name of the node.
      example: "proxmox_node"
      selector:
        text:
reboot_vm:
  description: Reboot a virtual machine in Proxmox.
  fields:
    vm_id:
      name: VM ID
      description: The ID of the virtual machine.
      example: "101"
      selector:
        text:
    node_name:
      name: Node Name
      description: The name of the node.
      example: "proxmox_node"
      selector:
        text:
shutdown_vm:
  description: Shutdown a virtual machine in Proxmox.
  fields:
    vm_id:
      name: VM ID
      description: The ID of the virtual machine.
      example: "101"
        text:
    node_name:
      name: Node Name
      description: The name of the node.
      example: "proxmox_node"
        text:
reset_vm:
  description: Reset a virtual machine in Proxmox.
  fields:
    vm_id:
      name: VM ID
      description: The ID of the virtual machine.
      example: "101"
      selector:
        text:
    node_name:
      name: Node Name
      description: The name of the node.
      example: "proxmox_node"
      selector:
        text:
pause_vm:
  description: Pause a virtual machine in Proxmox.
  fields:
    vm_id:
      name: VM ID
      description: The ID of the virtual machine.
      example: "101"
      selector:
        text:
    node_name:
      name: Node Name
      description: The name of the node.
      example: "proxmox_node"
      selector:
        text:
hibernate_vm:
  description: Hibernate a virtual machine in Proxmox.
  fields:
    vm_id:
      name: VM ID
      description: The ID of the virtual machine.
      example: "101"
      selector:
        text:
    node_name:
      name: Node Name
      description: The name of the node.
      example: "proxmox_node"
      selector:
        text:
